<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/public/styles.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Document</title>
  </head>
  <body>
    <%- include('includefile/header') %>
    <main>
      <% if (numberOfProfile == 0) { %>
      <p style="text-align: center; margin-top: 32px; font-size: 2.5rem">
        <span  class="animated-span">😥</span>Unfortunately , we found <span style="color: brown " >0</span>  enteries
      </p>
      <% } else { %>
      <h2 style="text-align: center">
        We found
        <span style="color: brown"><%= numberOfProfile %> </span>enteries
      </h2>
      <ul class="preview-data">
        <% for(const userIndividualData of userstoreddata) { %>
        <div class="card">
          <div class="card-header"><%= userIndividualData.usersname %></div>
          <div class="card-body">
            <blockquote class="blockquote mb-0">
              <p><%= userIndividualData.aboutUser %></p>
            </blockquote>
          </div>
        </div>

        <% } %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
